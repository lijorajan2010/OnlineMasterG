
@model IEnumerable<OnlineMasterG.Models.DAL.ExamSection>

<script type="text/javascript">

    $(document).ready(function () {
        $("#tblSections").DataTable();
    });
</script>

<table id="tblSections" class="dataTables_wrapper dt-bootstrap4 no-footer">
    <thead>
        <tr>
            <th style="width:30px;">Sl No.</th>
            <th>Section Name</th>
            <th style="width:50px;">Sequence</th>
            <th style="width:50px;">Is Active</th>
            <th style="width:50px;">Edit</th>
            <th style="width:50px;">Delete</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count() > 0)
        {
            foreach (var item in Model.Select((x, y) => new { Data = x, Index = y }))
            {
                <tr>
                    <td>@(item.Index + 1)</td>
                    <td>@item.Data.SectionName</td>
                    <td>@item.Data.Sequence</td>
                    <td>@(item.Data.Isactive == true ? "Active" : "Inactive")</td>
                    <td><button class="btn btn-info" onclick='editSection(@Html.Raw(Json.Encode(new { id=item.Data.SectionId,sectionName = item.Data.SectionName,sequence = item.Data.Sequence, isActive = item.Data.Isactive,languagecode = item.Data.LanguageCode })));'><i class="fa fa-pencil-alt"></i></button></td>
                    <td><button class="btn btn-danger" onclick='deleteSection(@Html.Raw(Json.Encode(new { id=item.Data.SectionId,sectionName = item.Data.SectionName})))'><i class="fa fa-trash"></i></button></td>
                </tr>

            }

        }

    </tbody>
</table>
